<?php $post = $wp_query->post; if ( in_category('70') ) 
{ include(TEMPLATEPATH . '/single-baskanlik-tasarim.php'); } 
elseif ( in_category('68') ) 
{ include(TEMPLATEPATH . '/single-baskanlik-tasarim.php'); } 
else { include(TEMPLATEPATH . '/single-default.php'); } ?>
